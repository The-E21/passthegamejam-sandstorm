using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DebugAbilities : MonoBehaviour
{
    public Ability ability1;
    public Ability ability2;

    // Start is called before the first frame update
    void Start()
    {
        /*var abilities = FindObjectOfType<PlayerAbility>();
        if (ability1) abilities.SetAbility(ability1, 1);
        if (ability2) abilities.SetAbility(ability2, 2);*/
    }
}
